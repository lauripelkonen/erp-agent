# Backend API URL (server-side only - used by API routes to proxy to your offer-agent)
BACKEND_URL=http://localhost:8000

# Public API URL (client-side)
# Leave empty to use this app's built-in API routes (recommended)
# Or set to your backend URL if calling directly from the browser
NEXT_PUBLIC_API_URL=

# Production example with Vercel:
# BACKEND_URL=https://your-offer-agent.aws.region.com
# NEXT_PUBLIC_API_URL=
